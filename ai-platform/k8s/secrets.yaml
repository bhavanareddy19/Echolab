# =============================================================================
# KUBERNETES SECRETS
# =============================================================================
# IMPORTANT: In production, use External Secrets Operator or HashiCorp Vault
# Never commit real secrets to git. These are placeholders.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: echolab-secrets
  namespace: echolab
type: Opaque
stringData:
  openai-api-key: "sk-placeholder-replace-in-production"
  postgres-password: "echolab_secret_2024"
  hf-token: "hf_placeholder-replace-in-production"
