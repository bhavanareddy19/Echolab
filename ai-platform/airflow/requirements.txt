# =============================================================================
# ECHOLAB AIRFLOW DEPENDENCIES
# =============================================================================
# All ML/AI libraries needed by our Airflow DAGs (pipeline orchestration)
#
# INTERVIEW TIP: These are the exact libraries you'll discuss in interviews.
# Know what each one does and WHY you chose it.
# =============================================================================

# --- LLM Frameworks ---
langchain==0.3.14                 # LLM application framework (chains, agents, tools)
langchain-openai==0.3.0           # OpenAI integration for LangChain
langchain-community==0.3.14       # Community integrations (vector stores, tools)
langgraph==0.2.60                 # Stateful multi-step LLM workflows (graph-based)
langchain-qdrant==0.2.0           # Qdrant vector store integration

# --- Multi-Agent Frameworks ---
crewai==0.86.0                    # Multi-agent orchestration (CrewAI)
pyautogen==0.4.0                  # Microsoft AutoGen multi-agent framework

# --- LLM APIs ---
openai==1.58.1                    # OpenAI GPT-4 API client
tiktoken==0.8.0                   # OpenAI tokenizer (for token counting)

# --- Vector Databases ---
faiss-cpu==1.9.0                  # Facebook AI Similarity Search (10M+ embeddings)
qdrant-client==1.12.1             # Qdrant vector DB client
chromadb==0.5.23                  # ChromaDB (alternative vector store)

# --- Embeddings & Transformers ---
sentence-transformers==3.3.1      # Sentence embeddings (all-MiniLM-L6-v2, etc.)
transformers==4.44.2              # Hugging Face transformers (compatible with tokenizers 0.20.3)
torch==2.5.1                      # PyTorch (CPU version for Airflow workers)
tokenizers==0.19.1                # Fast tokenizers (compatible with both chromadb and transformers)

# --- Fine-Tuning (QLoRA) ---
peft==0.14.0                      # Parameter-Efficient Fine-Tuning (LoRA, QLoRA)
bitsandbytes==0.45.0              # INT8/INT4 quantization for LLMs
datasets==3.2.0                   # Hugging Face datasets
trl==0.11.4                       # Transformer Reinforcement Learning (compatible with transformers 4.44.2)
accelerate==1.2.1                 # Distributed training utilities

# --- ML/Data Science ---
scikit-learn==1.6.0               # KMeans clustering, metrics
numpy==1.26.4                     # Numerical computing (compatible with langchain<2)
pandas==2.2.3                     # Data manipulation
scipy==1.14.1                     # Scientific computing

# --- Database ---
psycopg2-binary==2.9.10           # PostgreSQL driver
sqlalchemy==1.4.53                # SQL ORM (compatible with Airflow 2.8.1 <2.0 and langchain >=1.4)
pgvector==0.3.6                   # pgvector Python client
asyncpg==0.30.0                   # Async PostgreSQL driver

# --- Caching ---
redis==5.2.1                      # Redis client for caching layer

# --- Monitoring & Utilities ---
prometheus-client==0.21.1         # Metrics collection
python-dotenv==1.0.1              # Environment variable management
httpx==0.28.1                     # Async HTTP client
pydantic==2.10.4                  # Data validation
